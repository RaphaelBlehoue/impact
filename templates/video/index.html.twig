{% extends 'admin.html.twig' %}
{% block title %}{{ 'Section videos' }}{% endblock %}
{% block body %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'Section video'|upper }}</h4>
                <a class="btn btn-label btn-info" href="{{ path('video_new') }}">
                    <label><i class="ti-plus"></i></label>
                    {{ 'Ajouter le contenu'}}
                </a>
            </header>
            <div class="card-body">
                <table class="table table-striped table-bordered" cellspacing="0" data-provide="datatables">
                    <thead>
                    <tr>
                        <th>{{ 'Lien de la video' }}</th>
                        <th>actions</th>
                    </tr>
                    </thead>
                    <tfoot>
                    <tr>
                        <th>{{ 'Lien de la video' }}</th>
                        <th>actions</th>
                    </tr>
                    </tfoot>
                    <tbody>
                    {% for video in videos %}
                        <tr>
                            <td>{{ video.link }}</td>
                            <td>
                                <a href="{{ path('video_show', {'id': video.id}) }}" class="btn btn-primary">{{ 'Afficher' }}</a>
                                <a href="{{ path('video_edit', {'id': video.id}) }}" class="btn btn-danger">{{ 'Modifier' }}</a>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="6">no records found</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}