{% extends 'admin.html.twig' %}
{% block title %} {{ 'section nos metiers'|upper }} {% endblock %}
{% block body %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'Modifier section metiers'|upper }}</h4>
                {{ include('metier/_delete_form.html.twig') }}
            </header>
            <div class="card-body center-vh">
                {{ include('metier/_form.html.twig', {
                    'button_label': 'Mise à jour', 'button_class': 'btn-danger'
                })
                }}
            </div>
        </div>
    </div>
{% endblock %}