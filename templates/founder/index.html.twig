{% extends 'admin.html.twig' %}
{% block title %}{{ 'Section fondateur' }}{% endblock %}
{% block body %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'Section fondateur'|upper }}</h4>
                <a class="btn btn-label btn-info" href="{{ path('founder_new') }}">
                    <label><i class="ti-plus"></i></label>
                    {{ 'Ajouter le contenu'}}
                </a>
            </header>
            <div class="card-body">
                <table class="table table-striped table-bordered" cellspacing="0" data-provide="datatables">
                    <thead>
                    <tr>
                        <th>{{ 'Nom' }}</th>
                        <th>{{ 'Poste' }}</th>
                        <th>actions</th>
                    </tr>
                    </thead>
                    <tfoot>
                    <tr>
                        <th>{{ 'Nom' }}</th>
                        <th>{{ 'Poste' }}</th>
                        <th>actions</th>
                    </tr>
                    </tfoot>
                    <tbody>
                    {% for founder in founders %}
                        <tr>
                            <td>{{ founder.name }}</td>
                            <td>{{ founder.office }}</td>
                            <td>
                                <a href="{{ path('founder_show', {'id': founder.id}) }}" class="btn btn-primary">{{ 'Afficher' }}</a>
                                <a href="{{ path('founder_edit', {'id': founder.id}) }}" class="btn btn-danger">{{ 'Modifier' }}</a>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="8">no records found</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}