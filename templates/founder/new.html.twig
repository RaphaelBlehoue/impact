{% extends 'admin.html.twig' %}
{% block title %} {{ 'section Fondateur'|upper }} {% endblock %}
{% block body %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'section Fondateur'|upper }}</h4>
                <a class="btn btn-square btn-outline-danger" href="{{ path('founder_index') }}">
                    <label><i class="ti-na"></i></label>
                </a>
            </header>
            <div class="card-body center-vh">
                {{ include('founder/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}