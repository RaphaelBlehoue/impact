{% extends 'admin.html.twig' %}
{% block title %} {{ 'Modifier PAGE ADN' }} {% endblock %}
{% block body %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'Modifier PAGE ADN'|upper }}</h4>
                {{ include('ipadn/_delete_form.html.twig') }}
            </header>
            <div class="card-body center-vh">
                {{ include('ipadn/_form.html.twig', {
                    'button_label': 'Mise à jour', 'button_class': 'btn-danger'
                })
                }}
            </div>
        </div>
    </div>
{% endblock %}