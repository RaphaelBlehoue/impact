{% extends 'admin.html.twig' %}
{% block title %} {{ 'section contact'|upper }} {% endblock %}
{% block body %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'section contact'|upper }}</h4>
                <a class="btn btn-square btn-outline-danger" href="{{ path('information_index') }}">
                    <label><i class="ti-na"></i></label>
                </a>
            </header>
            <div class="card-body center-vh">
                {{ include('information/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}