{% extends 'admin.html.twig' %}
{% block title %}{{ 'Section approche' }}{% endblock %}
{% block body %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'Section approche'|upper }}</h4>
                <a class="btn btn-label btn-info" href="{{ path('approch_new') }}">
                    <label><i class="ti-plus"></i></label>
                    {{ 'Ajouter le contenu'}}
                </a>
            </header>
            <div class="card-body">
                <table class="table table-striped table-bordered" cellspacing="0" data-provide="datatables">
                    <thead>
                    <tr>
                        <th>{{ 'Titre' }}</th>
                        <th>actions</th>
                    </tr>
                    </thead>
                    <tfoot>
                    <tr>
                        <th>{{ 'Titre' }}</th>
                        <th>actions</th>
                    </tr>
                    </tfoot>
                    <tbody>
                    {% for approch in approches %}
                        <tr>
                            <td>{{ approch.title }}</td>
                            <td>
                                <a href="{{ path('approch_show', {'id': approch.id}) }}" class="btn btn-primary">{{ 'Afficher' }}</a>
                                <a href="{{ path('approch_edit', {'id': approch.id}) }}" class="btn btn-danger">{{ 'Modifier' }}</a>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="7">no records found</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}