{% extends 'admin.html.twig' %}
{% block title %} {{ 'CONTENU ADN' }} {% endblock %}
{% block body %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'CONTENU ADN'|upper }}</h4>
                <a class="btn btn-square btn-outline-danger" href="{{ path('ip_item_adn_index') }}">
                    <label><i class="ti-na"></i></label>
                </a>
            </header>
            <div class="card-body center-vh">
                {{ include('ip_item_adn/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}