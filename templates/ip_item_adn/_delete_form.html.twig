<form method="post" action="{{ path('ip_item_adn_delete', {'id': ip_item_adn.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ip_item_adn.id) }}">
    <button class="btn btn-float btn-danger"><i class="ti-trash"></i></button>
</form>