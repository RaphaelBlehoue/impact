{% extends 'admin.html.twig' %}
{% block title %} {{ 'Catégorie Articles' }} {% endblock %}
{% block body %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'Creér une catégorie d\'article'|upper }}</h4>
                <a class="btn btn-square btn-outline-danger" href="{{ path('subject_index') }}">
                    <label><i class="ti-na"></i></label>
                </a>
            </header>
            <div class="card-body center-vh">
                {{ include('subject/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}